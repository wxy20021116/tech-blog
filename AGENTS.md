# AGENTS.md

本文件用于说明在本项目中工作的约定。

## 工作方式

- 完成代码或文章修改后，不需要执行完整编译、构建或测试。
- 最多只使用轻量语法检查命令确认没有明显语法错误，例如 `pnpm astro check`、`pnpm check`、`pnpm type-check` 或针对单个文件的格式/语法检查。
- 改动不多时，用 `check` 类命令检查语法问题；改动较多时，可以本地编译测试，但不要启动本地服务。
- 不要主动运行 `pnpm build`，也不要等待本地构建结果。

## 提交与部署

- 修改完成后直接提交代码。
- GitHub 仓库已配置自动流程，推送代码后会自动触发 Cloudflare Pages 部署。
- 只需要把代码提交并推送到 GitHub；不用额外处理 Cloudflare，也不用等待部署完成。
